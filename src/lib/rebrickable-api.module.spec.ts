import { describe } from "@jest/globals";
import { RebrickableApiModule } from "./rebrickable-api.module";

describe(RebrickableApiModule, () => {
  it("runs test", () => {
    expect(true).toBeTruthy();
  });
});
